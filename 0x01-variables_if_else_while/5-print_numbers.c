#include <stdio.h>

/**
 * main - main function
 *
 * Return: always 0
 *
 */

int main(void)
{
	char(letters);

	for (letters = 0 ; letters < 10 ; letters++)
		printf("%d", letters);
	printf("\n");
	return (0);
}
