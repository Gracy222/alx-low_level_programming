#include "main.h"

/**
 * _strspn - that gets the length of a prefix substring.
 * @s: parameter 1
 * @accept: parameter 2
 *
 * Return: s
 */
unsigned int _strspn(char *s, char *accept)

{
	char *s = p

	while (*s)
	{
		q = accept;

		while (q && q != *p)
		{
		p++;
		}
		if (i == '\0')
		{
			break;
		}
		q++;
	}
	return (q -p);
}

